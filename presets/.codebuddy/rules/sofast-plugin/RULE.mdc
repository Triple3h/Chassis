---
description: 如快 Sofast 插件开发硬约束：宿主调用必须加超时、命令名与产物文件名必须一致、shared 依赖解析、性能与安全的红线
alwaysApply: true
enabled: true
provider:
---

# 如快插件开发铁律

本仓库全部代码都是如快 Sofast 插件。下面每一条都是「不遵守就一定出问题」的硬约束。
完整背景与踩坑复盘见 `docs/sofast-plugin-dev-guide.md`；新建插件的操作流程见 skill `sofast-plugin-dev`。

## 宿主能力

- 所有宿主 API（`Context` / `Screenshot` / `LocalStorage` / `Backend` / `Quicklink`）**必须**经 `shared/lib/platform.ts` 调用，禁止在组件里裸调 `@sofastapp/api`。宿主不在时 SDK 的 Promise 永不 resolve，没有超时会卡死插件启动。
- 剪贴板读取优先用 `paste` 事件的 `clipboardData`（用户手势，无需权限）；写入用 `writeText` → `execCommand` 三级兜底；UI 永远保留手工操作路径。
- 密钥、验证码等敏感数据默认不落明文；要持久化就提供口令加密（参考 `presets/sofast-totp/src/core/vault.ts`）。

## 清单与产物

- `commands[].name` 对 `no-view` / `script` 命令**必须**等于产物文件名（`dist/<name>.mjs`）。
- worker 构建配置**必须** `emptyOutDir: false`，且构建顺序是先 UI（`vite build`）后 worker。
- 有**多个** `no-view` / `script` 入口时**禁止** Rollup 多入口一次构建：被两个入口共用的模块会被拆成 `dist/assets/*.mjs`，入口里只剩一条相对 import，宿主只认 `dist/<name>.mjs`，这条依赖断了整个命令就失效。必须逐入口各跑一次构建（`build.lib.entry` 指向单个入口），保证每个 `.mjs` 自包含（参考 `presets/sofast-hosts/scripts/build-no-view.mjs`）。
- `vite.config.ts` **必须** `base: './'`，且不要启用 `manualChunks`。
- `dist/` 必须包含 `index.html` + `assets/` + `package.json`（含完整 `commands`）。

## 模块解析

- `shared/` 在插件 `node_modules` 之外：新增插件时**必须**同时配 Vite `resolve.alias`（用 `scripts/vite-shared.mjs`）、tsconfig `paths`、dev server 的 `server.fs.allow` 三处。
- Tailwind v4 不会跨界扫描：`src/styles/app.css` **必须**用 `@source` 声明插件 `src` 与 `shared` 两个范围，否则类名不生成。

## 性能

- 解析、差分、格式化等 O(n) 以上或可能超过 50ms 的计算**必须**进 Web Worker，并保留主线程降级分支。
- 可能超过 200 行的列表**必须**虚拟滚动（用 `shared/lib/virtual.ts`，定高行不需要测量）。
- 大文本/大 JSON 禁止 `JSON.parse` + `stringify` 往返（会丢数字精度与转义细节）；需要结构时用手写词法扫描。
- 表示树形结构**禁止**假设「兄弟节点下标连续」（先序遍历下不成立），用 `firstChild` + `nextSibling`。

## 收尾

- 改完**必须**跑：插件目录下 `npm run typecheck && npm run build && npm test`。
- 交付前**必须**用静态服务器打开 `dist/` 实机点一遍（生产构建的资源路径与 dev server 不同）。
- 新增 `no-view/` 脚本时，同步更新 `package.json` 的 `commands` 与 `build` 脚本。
