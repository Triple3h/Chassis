/**
 * 图标：表已上收到 `@launcher/ui`（`packages/ui/lib/icons.ts`）—— 设置页的插件列表
 * 也要按同一份 lucide 表渲染插件 logo，同一个插件在两处必须是同一个图标。
 * 这里只做转发，调用方（`components/IconGlyph.vue`）不用改 import 路径。
 */
export { ICONS, iconMarkup, iconSvg } from '@launcher/ui/icons'
