---
description: 底座（apps/ 与 packages/）开发硬约束：三铁律职责边界、Rust 工程与协议（workspace 布局/camelCase 契约/读循环不许内联 await/日志只走 stderr）、窗口显隐/拖动/缩放与选区时机、配置写入收口、宿主↔插件边界（postMessage/主题/CSP/退出）、插件策略（essential/history/别名/清单字段三处同步）、插件装载与平台过滤（platforms/arch，扫描期跳过、非法声明不静默吞）、数据迁移、发布与更新通道（App v* / 应用自更新 app-latest / 内核 kernel-latest / 插件 plugins-latest 四条独立通道、单独发布必须合并索引、CI 不监控 main、应用自更新签名硬前提）与样式同源
alwaysApply: true
enabled: true
provider:
---

# 底座开发铁律（apps/ 与 packages/）

`apps/shell`（Rust 壳）/ `apps/kernel`（Rust 内核，bin `launcher-kernel`；ADR-0005）/ `apps/launcher-ui`（Vue UI）/ `packages/*` 的硬约束。
**内核、SDK 与逻辑层插件已是 Rust；UI 与视图层插件仍是 Vue/TS** —— 下文反引号里的符号名默认指 Rust 侧，UI 侧保留原名（如 `showWindowAnimated()`）。
每条都是「不遵守就一定出问题」的；原理与复盘见 `docs/architecture.md` 与 `docs/decisions/`（ADR-0001~0005）。
插件侧约束见规则 `chassis-plugin`；收尾验证见技能 `chassis-deliver`。

## 职责边界（三铁律）

- **内核零能力**：扫描应用 / 读写文件 / 启动 / 连网全在插件；内核 diff 里出现能力词（app / file / network）= 越界。
- 出厂插件与第三方插件走**同一机制**（可禁用可卸载）；未声明 capability 的调用装配期就不挂载。
- **协议只走 stdout，日志一律 stderr**（壳 ↔ 内核 = stdio + newline JSON-RPC；逻辑层插件 = spawn + NDJSON，同款约定）。
- **逻辑层插件（v2，M5 起）= 可执行产物**：宿主 `spawn`，`.mjs` 产线不再支持（ADR-0005）；`commands[].name` = 产物文件名（`dist/<name>`，Windows `.exe`），查找顺序 `<name>(.exe)` → `workers/<name>(.exe)`。
- 新增槽位 / 字段 / 协议行为：先改 `docs/launcher-requirements.md`（需求唯一源）与 `docs/plugin-spec.md`（对外契约），再实现。

## Rust 工程与协议（内核 / SDK / 逻辑层插件）

- **workspace 在仓库根 `Cargo.toml`**：成员逐个列出（`packages/plugin-sdk-rs`、`apps/kernel`、各逻辑层插件 `plugins/<id>`）—— 不能写 `plugins/*` glob（会匹配到没有清单的纯 view 插件目录而报错）；`apps/shell` 是独立 workspace，必须显式 `exclude`；产物统一在根 `target/`。**新增逻辑层插件 = 建 crate + 登记 members**，两步缺一不可。
- **跨协议 JSON 字段一律 camelCase**：Rust 结构体一律 `#[serde(rename_all = "camelCase")]`，字段名与 `docs/plugin-spec.md` 逐字段对齐（「协议不变只换载体」的落实点）。契约有两份实现（内核 `manifest.rs` / `contract.rs` ↔ TS 包 `packages/plugin-manifest`），改字段两边必须同步。
- **壳 ↔ 内核的读循环不能自己堵自己**：handler 里**必须 `tokio::spawn`**，绝不能内联 `await` 一个「发链路请求、等壳回」的调用 —— 回复正是由读循环读的，内联 await = 自己等自己（必然超时，且期间所有壳往来全部堵死）。回归用例 `apps/kernel/src/link.rs::handler_may_await_link_requests`，改 `link.rs` 必须让它继续绿。
- 内核日志一律 stderr（`logging.rs` 收口）；**stdout 只有一条通道 `link::stdout_output()`**（`lib.rs` 注入 `Kernel.link_writer`），其余代码任何裸 `println!` 都等于污染协议。
- Rust 测试：单测就近模块内 `#[cfg(test)] mod tests`，跨模块 / 协议 / 装配放 `apps/kernel/tests/`；一把跑 `cargo test --workspace`（内核 / SDK / 各插件 crate 全覆盖）。跨进程断言用 `waitFor`（HTTP 响应先于 SSE 事件到达）。
- **依赖克制**：内核 / 插件依赖是精挑过的（tokio 只开用到的 feature、`sysinfo` 关默认特性）。新增第三方 crate 要说明用途并锁最小 features；能用 std 就不引。
- **平台限定逻辑必须隔离**：`mdfind` / `sips` / `osascript` / AX API 等 macOS-only 实现放 `#[cfg(target_os = "macos")]` 分支并在 `plugin-spec` 标注；M6（Windows）要补第二套后端，通用路径里不许假设 POSIX。

## 窗口显隐与动效（踩坑最多，动它前读完）

- 唯一裁决者是壳：壳 `window/toggled` → 内核只广播；**任何真改了显隐的路径都必须回报**（托盘 / 单实例 / ⌘W 都补齐过；漏报 = 唤出是空窗口），显示失败也要广播。
- 排队中的隐藏可被 `cancel_pending_hide()` 撤销（内核 `window_visibility.rs` 用令牌作废迟到的回执；壳侧同名函数清 `HIDE_AT_MS`），**迟到的回执也要作废**；`is_visible() -> Option<bool>`（「问不到」≠「否定」）。
- **显示**要等窗口真能画：窗口隐藏时 webview 不绘制、CSS 时钟照走 ⇒ 内核 `emit_visible_animated()` 推迟 `SHOW_ANIMATION_MS`(80ms) 广播；UI `setWindowVisible()` 等可见信号 + `waitForPaint()`（页面可见后两帧；120ms 上限 + setTimeout 保险）。
- **隐藏一律等 UI 回执才落地**：`emit(false)` → UI 演离场 → `POST /api/window/hidden` → 内核 `finish_hide()` → 落地；兜底 `HIDE_FALLBACK_MS` 500ms（内核）/ 800ms（壳）。
- 壳里 `window/hide` 落地后**必须清 `HIDE_AT_MS`**（否则下次热键被当成「取消隐藏」）；`toggle()` 的「离场途中再按 = 取消隐藏」判断排最前；UI `REVEAL_FAST_MS`(180ms) 必须小于「离场 + 回执」耗时。
- **窗口边角**：`.shell` 上不放 `backdrop-filter`（唯一会溢出圆角的属性），也不放 `box-shadow`（阴影全在窗口外、一个像素都渲染不出来，却会让 WebKit 把贴住视口底边的圆角画成直角）；`clip-path: inset(0 round var(--radius-shell))` 兜底。
- 动效令牌单一来源（ADR-0004）：UI `--motion-*`、插件侧镜像 `--launcher-motion-*`；`show/hideWindowAnimated()` 是唯一收口。

## 窗口拖动 / 缩放 / 选中文本 / 状态条

- **选区只能在 `window.show` 之前读**：窗口一上屏，前台 App 就是自己，`AXSelectedText` 随之消失（`primitives/selection.rs`）。所以 `window.show` 的返回值带 `{selection}` —— **谁调 show 谁负责把它转交内核**；热键 / 托盘 / 单实例三条路统一走 `window::show_and_report`（`let _ = show(...)` ＝ 选中文本凭空消失，"选中了再按热键却不带"就是这么来的）。
- 读取策略在内核（`Kernel::apply_selection`，`kernel.rs`）：只填**空搜索框**、超长（`SELECTION_MAX_CHARS`）不带；壳只管「此刻前台选中了什么」这个事实。
- **窗口尺寸归 UI**：把手按下 = `manualResized`，内容高度不再改窗口；尺寸**按模式分别记忆**（`config.windowSizes.host` / `.plugin`，UI 提交、内核 `sanitize_window_sizes()`（`config.rs`）钳制），下次唤出 / 进插件页 / UI 挂载时用 `window.setSize` 还原（自适应那条路仍走 `window.setHeight`，别混用）。落盘时机：拖完防抖 400ms + **离场前 `flushSizeSave()` 补一次**；「恢复默认大小」= 清当前模式的记忆 + `setWindowHeight(desiredHeight, { force: true })` 回自适应。写回时必须**整个 `windowSizes` 对象一起给**（`patch_config` 是浅合并，少给一个键就把另一模式的记忆抹了）。
- `-webkit-app-region: drag` 只准用在**没有交互元素**的条带上（当前只有顶缘 8px 热区）；含 input/button 的行一律走 `data-drag-region` + `window.startDragging`（IPC 兜底），别赌某个 WebKit 版本的 `no-drag` 实现。
- 缩放把手 `z-index` 必须高于拖拽条（角上缩放优先于拖动）；边把手宽度保持 6px（再宽会压住插件页 iframe 的滚动条）。
- 「恢复默认大小」按钮有两个落点：宿主态在搜索栏右侧（只有图标）、插件页在宿主 footer 右侧（带文字）—— 插件页没有搜索栏可挂，别只在一边加。
- 无边框窗口的系统调用：`start_resize_dragging` 只长在 `Window` 上（`WebviewWindow` 没有）⇒ `main_window(app)?.as_ref().window()`；方向类型来自 `tauri-runtime`（版本必须与 tauri 依赖的一致）。
- 状态条（CPU / 内存）是**内核自用**数据（`apps/kernel/src/services/system_stats.rs`，`sysinfo` 差分采样），**不进插件 API**；UI 显示**绝对量**而不是百分比（macOS `freemem` 不含可回收缓存，百分比常年八九十，写出来只会吓人）。

## 配置与事件

- **配置写入只有一个收口 `Kernel::patch_config()`**（`apps/kernel/src/kernel.rs`）：落盘 → 副作用（历史上限 / 自启 / 热键）→ `emit('config/changed')`。`POST /api/config`、管理面 `ctx.settings.patch`、`setAutostart` / `setHistoryLimit` 全部转发到它 —— **别在调用点上补广播**。
- 广播的 UI 侧配套三处：`lib/api.ts` 事件名单、`stores/data.ts` 用 payload 更新快照、`App.vue` watch；守卫 `tests/unit/config-events.test.ts`（每条路径一条用例）。
- 验证必须走**真实路径**：设置页走的是 `ctx.settings.patch`，`curl /api/config` 会恰好绕开坏的那条。

## 宿主 ↔ 插件边界

- **postMessage 是结构化克隆**：Vue 的 `reactive` / `ref`（Proxy）混进 payload 抛 `DataCloneError` ⇒ 消息根本发不出去（表现为「点按钮没反应、审计无记录、SDK 只超时」）。SDK `post()` 已内置 JSON 展平兜底；插件侧写库前 `map((x) => ({ ...x }))`。
- **插件页主题**：手动 > 宿主 `?theme=` > `data-theme` > 系统；**只有手动切换才写 localStorage**（自动判定落盘 = 把首次打开时的主题永久钉死）；系统变化只在来源是 system 时跟随。
- **插件页 CSP**：`script-src 'self' 'wasm-unsafe-eval'`（放行随包 wasm 编译，必须随包、禁 CDN；JS `eval` 仍禁）；受 `apps/kernel/src/http/plugin_servers.rs` 的 `plugin_page_csp_allows_wasm_but_not_eval` 守护。
- **插件重载会打死它自己的插件页**：`session/closed{reason}` + `plugin/reloaded` ⇒ UI 用同一命令换新会话重开；其余 reason 直接卸载 iframe。
- **插件视图的退出入口是宿主的责任**（顶栏由插件自己画）：footer 最左「返回」+ `Esc` / `⌘W`，三条路径统一走 `leavePluginView()`（close → `nextTick()` → focus）。焦点在 iframe 里时宿主收不到按键（事件不跨文档冒泡）⇒ **SDK 把没人消费的 Esc 交还宿主**（`commands.close` → `session/closed{reason:'ui'}` → 同一条收尾，含焦点归位）；插件消费 Esc 要 `preventDefault()` / `stopPropagation()`。受 `tests/unit/plugin-api-escape.test.ts` 守护。
- view 会话打开时宿主**卸载搜索框** ⇒ `hostUi.watchSearchContent` 不触发；入口型插件用 `hostUi.getSearchContent()` 取初始输入；footer 的 keys 只展示、不派发。

## 插件策略（内核侧）

- **`essential`（不可禁用）**：只认**出厂身份表**（`PluginManager::collect_builtin_identity()`，`scan()` 时从出厂 bundle 的清单读出 `id → essential`），`is_essential = builtin && 身份表[id]`（第三方写了无效，**被覆盖的版本写了也无效** —— 否则改一下清单就白拿「不可禁用 + 免审计」）；身份表查不到时按 `true` 处理（保守）；启动期 `prune_essential_disabled()`（`plugin/manager.rs`）清配置里的残留禁用；其调用**不进审计**（`audit.set_exempt`）。
- **插件可被覆盖更新（M7，有意为之的例外）**：`extensions/<id>/` 能覆盖**非 essential** 出厂插件（`scan()`），覆盖后 `builtin` 保持 true（不可卸载、可禁用）；essential 的忽略 extensions 版本并记 warn。覆盖目录清单读不出 / 非法 / 平台不合法 ⇒ 回落到出厂版本。安装一律走 `install_prepared()`：**staging → disable(Reload) → 旧版整目录备份到 `.backup/<id>/<版本>（只留 1 份）→ rename 落地 → scan + load**，load 失败自动回滚并报 `UPDATE_FAILED`；**绝不走 `uninstall` 路径**（那条会清 `plugin-overrides` 与 `plugin-settings`）。出厂插件的「卸载」等价动作是 `pluginAction('revertToBuiltin')`。
- **`history: false`（不进最近使用）**：写历史前判 `PluginManager::excludes_history(id)`（`Kernel::invoke` 与 `rememberItemResult` 两处）**且**启动装配期 `history.drop_history_by(...)` 摘掉存量条目（只「以后不写」的话，界面上旧记录还在 = 用户以为没生效）；只动历史、**不动固定项**。
- **清单字段白名单只有一份**：`scripts/lib/manifest-keys.mjs`（两套构建器 + spec-check 共用），加字段先动它。新字段要**三处一起落**：白名单 + 内核 `manifest.rs` + TS 契约包 `packages/plugin-manifest`（少一处 = 字段被静默裁掉或两边校验口径不一致）。
- **搜索命中面 = 命令 `title` / `subtitle` / `keywords`**（`pinyin::match_target`：前缀 1.0 / 包含 0.7 / 全拼 0.6 / 首字母 0.5 / subtitle 与 keywords 0.4；多音字按 `ToPinyinMulti` 展开读音变体、封顶 8 个，改打分要跑 `pinyin.rs` 的用例组）；别名两层 = 插件级（兜底全部入口命令）∪ 命令级；用户改动存 `<dataRoot>/plugin-overrides.json`（`undefined` = 用清单值 / `[]` = 显式清空），改完只 `registry.update` 即时生效、**不重载插件**。
- 固定 / 历史项必须存 `action` 快照：应用 / 文件 / 网址的 `command` 是**结果项 id**（不是命令名）；UI `pinPayload` 要带 `result.item.action`。

## 内置原生插件（internal-settings）

- `ctx.settings` 只注入 `internal-` 前缀插件（ADR-0003）；`pluginAction` 有 `setKeywords` / `resetKeywords` / `enable` / `disable` / `setCapability` / `installZip` 等。
- 设置页插件 tab 是原生 DOM 单文件：主从两栏；事件**全走 panelEl 委托**（render 重建 innerHTML）；4s 轮询只在「摘要变了且非编辑态」才 render；chip 未落盘值放 `keywordEdits`、增删只重建局部容器 —— **别整体重渲染，会打断输入丢焦点**。

## 数据与插件装载

- **插件根识别**：`<dir>/dist/package.json` 存在 ⇒ 插件根 = `<dir>/dist`（`PluginManager::scan()` 与 `install_from_directory()` 必须一致，`plugin/manager.rs`）。
- **平台过滤（plugin-spec §3.5）**：清单可选字段 `platforms`（`macos`/`windows`/`linux`）与 `arch`（`x64`/`arm64`），**省略 = 不限制**（只增不减，老插件无感）。判定在 `scan()` 里（`platform_skip_reason()` + `manifest::raw_platform_mismatch()`），是**加载前**的唯一闸门 ⇒ 不匹配的插件不注册命令、不进设置页、不占历史、不启插件页服务；运行时**不做二次判定**。
  - **声明非法（`"windows"` / `[]` / `["win"]`）绝不静默跳过** —— 让它走到 `load()` 报 `MANIFEST_INVALID`，用户才看得见「清单写错了」（插件无声消失是最难排查的那一类问题）。
  - 安装期（`installDir` / `installZip`）另判一次并返回 `PLATFORM_MISMATCH`：显式动作必须有回执。
  - **口径**：`platforms` 用 Rust `std::env::consts::OS`（照 `#[cfg(target_os)]` 写），**不是** `host.info().platform` 的 Node 口径 `darwin` / `win32` —— 两者混用是最容易犯的错。
  - 不要在打包脚本（`scripts/lib/resources.mjs`）里按平台过滤：那会让 dev 与打包行为不一致。
- 数据布局：`<dataRoot>/{config,history,pinned}.json`、`plugin-overrides.json`、`extensions/<id>/`、`plugins/<id>/`（插件唯一可写处）、`logs/audit-*.jsonl`（滚动 7 天）。
- 插件 id 改名后老数据由内核接手：`PluginManager::adopt_legacy_data_dir()`（数据目录）+ `HistoryStore::migrate_plugin_ids()`（历史 / 固定项）。链在 `apps/kernel/src/legacy.rs`：`RENAME_CHAINS` = `[当前 id, ...历代旧 id]`；`legacy_data_dir_ids()` 给「新→旧」候选（逐个试），`legacy_id_to_current()` 是「旧→新」**单跳**查表（链断了会停在中间那一代）——**传错 = 把新条目改回旧 id**。
- 壳启动 `adopt_legacy_data_dir()`（`apps/shell/src/sidecar.rs`）从 `Launcher/` 一次性复制接手（只复制不移动），必须排在 `logging::init()` 之前 —— 否则新目录已存在、迁移永不执行。

## 发布与更新通道（App / 内核 / 插件）

- **三条通道互不干扰**：App `v*`、内核 `kernel-latest`、插件 `plugins-latest`（各自 `releases/download/<tag>/…` 直链；客户端不调 GitHub API、无 token）。**CI 不监控 main**：`verify.yml` 的 push 忽略 main，发布工作流全是 `workflow_dispatch` + 自己的 tag 前缀 ⇒ 合并 main 不触发任何构建/部署，发版必须显式触发。
- **单独发内核 / 插件不用动 App**：bump 版本（`apps/kernel/Cargo.toml` / 插件 `package.json`）→ 合并 main → `gh workflow run kernel-release.yml|plugins-release.yml --ref main`（workflow 取**默认分支**代码）。发布即对所有装机客户端可见（**无灰度**），门闩只有索引里的 `minHotVersion`（内核包 × 客户端机制版本）与 `minKernel`（插件 × 当前内核）。
- **插件索引永远整份发布**：单独发某些插件时必须合并上一版 `registry.json`（`gen-plugin-registry.mjs --merge-registry`，`plugins-release.yml` 已自动做，取不到索引直接红）：没打包的插件整条沿用，打包的插件按平台替换资产，未重建平台的旧资产只在版本一致时保留。漏了这步 ⇒ 索引里只剩本次打包的插件，其余插件静默停止更新（守 `tests/unit/plugin-registry-merge.test.ts`）。
- **App 升级 = 重置基准线**：壳把包内内核重投到 `<dataRoot>/kernel/`（台账 `kernel.json` 的 `sourceAppVersion`），内核热更新只是两次 App 发版之间的「领先」，不是替代。**应用（壳）自更新同理**（版本必变 ⇒ 外置内核跟着重投，属预期）。
- **应用（壳）自更新（`app-latest`，2026-09-19 起）**：整包替换 `.app`，仅 macOS（Windows 运行中 exe 被锁）。分工：检查 / 下载 / 解压在 `internal-store`（`check-app` / `download-app`），**候选包自检 / 台账 / 替换 / 重启在壳**（`apps/shell/src/update.rs` + 脱离壳进程树的 helper；`boot_guard` 连续两次未就绪自动回滚）。触发由内核守护掌握（启动 90s 后 / 每 6h，`config.autoUpdateApp` 可关）。**签名硬前提**：CI 用 secrets `MACOS_SIGN_P12`（本机固定证书，`node scripts/export-signing-cert.mjs` 导出）签名，否则每次自更新后 TCC 授权重弹。落点 `hot/shell/`，详见 `docs/shell-hot-update.md`。

## 样式

- **表单控件样式两处同源**：设置页 `index.html` 全局控件样式 ↔ `packages/ui/styles/theme.css` 的 `.launcher-input`（滚动条是「宿主 `app.css` + 这两处」的既有约定）。原生控件明暗靠 `color-scheme`（`:root` light / `[data-theme='dark']` dark）；focus 环一律 `--accent`（系统蓝不跟主题色）。
- **下拉框一律自绘**：`<select>` 的**弹出菜单**在 macOS 由系统（NSMenu）绘制 —— `appearance:none` / `color-scheme` 都改不动它（2026-09-16 实机截图确认）。Vue 插件用 `@launcher/ui/UiSelect.vue`（菜单 Teleport 到 body + fixed 定位 ⇒ 对话框的 `overflow:auto` 裁不到它）；设置页用 `.lselect` 结构 + `selectHtml()`（交互全在 panelEl 委托里）。两边行为必须对齐：↑↓ / Enter / Esc / Tab / 点外部与滚动关闭、上下翻转、`role=listbox|option` + `aria-selected`。
- 坑：`.lselect-menu { display: flex }` 会盖掉 `[hidden]` 的 UA `display:none` ⇒ 必须显式补 `.lselect-menu[hidden] { display: none }`（实机才会发现的「关了还开着」）。
