---
description: Vue 插件（plugins/{totp,hosts,text-diff,json-tools}）开发硬约束：宿主调用直连 SDK、命令名与产物文件名必须一致、@launcher/ui 依赖解析、性能与安全的红线
alwaysApply: true
enabled: true
provider:
---

# Vue 插件开发铁律

`plugins/` 下 4 个 Vite + Vue 插件（totp / hosts / text-diff / json-tools）的硬约束。
下面每一条都是「不遵守就一定出问题」的；完整背景见 `docs/plugin-dev-guide.md`（历史与踩坑），
操作流程见 skill `chassis-plugin-dev`。

## 宿主能力

- 宿主 API **直连 SDK**：view 侧 `import { exec, host, hostUi, screenshot, storage } from '@launcher/api'`；
  script 侧 `import { ctx, done, fail, log, onError } from '@launcher/api-node'`。
  双宿主适配层已于 2026-09-16 退役，**不要**再引 `packages/ui/lib/platform`。
- 每次调用都要想清楚失败路径：SDK 的 Promise 以 `LauncherError` 拒绝（不在宿主里时立刻 `NOT_FOUND`，宿主无响应则超时）。
  需要「失败即空值」就在调用点接 `.catch(() => null)`；`host.isLauncher()` 是**同步**判断，先用它分流。
- 剪贴板读取优先用 `paste` 事件的 `clipboardData`（用户手势，无需权限）；写入用 `writeText` → `execCommand` 三级兜底；UI 永远保留手工操作路径。
- 密钥、验证码等敏感数据默认不落明文；要持久化就提供口令加密（参考 `plugins/totp/src/core/vault.ts`）。

## 清单与产物

- `package.json` **必须**提供 `build:view` / `build:scripts` 脚本：根 `scripts/build-all.mjs` 按这两条驱动构建
  （只有 view 就只写 `build:view`；有 no-view/script 才写 `build:scripts`），否则 `pnpm build` 会跳过该插件。
- `commands[].name` 对 `no-view` / `script` 命令**必须**等于产物文件名（`dist/<name>.mjs`）。
- worker 构建配置**必须** `emptyOutDir: false`，且构建顺序是先 UI（`vite build`）后 worker。
- 有**多个** `no-view` / `script` 入口时**禁止** Rollup 多入口一次构建：被两个入口共用的模块会被拆成 `dist/assets/*.mjs`，入口里只剩一条相对 import，宿主只认 `dist/<name>.mjs`，这条依赖断了整个命令就失效。必须逐入口各跑一次构建（`build.lib.entry` 指向单个入口），保证每个 `.mjs` 自包含（参考 `plugins/hosts/scripts/build-no-view.mjs`）。
- `vite.config.ts` **必须** `base: './'`，且不要启用 `manualChunks`。
- `dist/` 必须包含 `index.html` + `assets/` + `package.json`（含完整 `commands`）。

## 模块解析

- `@launcher/ui`（`packages/ui`）是工作区包：插件 `package.json` 声明 `"@launcher/ui": "workspace:*"` 后走标准解析，**不需要**再配 Vite alias / tsconfig paths。
- Tailwind v4 不会跨界扫描：`src/styles/app.css` **必须**用 `@source` 声明插件 `src` 与 `../../packages/ui` 两个范围，否则类名不生成。

## UI 与样式

- 表单控件写 `class="launcher-input"`（背景 / 边框 / focus 环跟着主题与主题色走）。
- **下拉框用 `@launcher/ui/UiSelect.vue`，不要写原生 `<select>`**：它的弹出菜单由 macOS 系统（NSMenu）绘制，不跟主题、不跟主题色。用法 `:model-value` + `@update:model-value`（`v-model` 可，数字字段自己 `Number()` 转）。
- 弹窗用 `@launcher/ui/UiDialog.vue`（自带遮罩 / Esc 关闭 / 进场），别自己叠一层。
- **消费 `Esc` 必须 `preventDefault()`**（`stopPropagation()` 也算）：没被消费的 Esc 由 SDK 自动交还宿主（退回启动台）。`UiDialog` / `UiSelect` 已内建，自己画的弹层 / 清空搜索框要照做。
- **进度类动画（倒计时环、进度条）只准有一个时间源**：JS 算目标值 + `transition` 补间；**不要**用 CSS 动画再每秒重设 `animation-delay`（动画自身时钟也在走 = 把同一秒算两遍，环一轮转两圈）。跨周期那一帧要临时关过渡，否则补间会把「空 → 满」演成倒转一整圈（参考 `plugins/totp/src/App.vue` 的 `ringOffset` / `markStepReset`）。
- 主题色派生的**半透明填充不要写 `color-mix`**：构建器（Lightning CSS）会把它拆成「实色兜底 + 真值」两条规则，实色兜底在老一点的 WebKit 上会把整块面刷成纯色（选中卡片直接变纯蓝）。写法：插件自己的 `:root` / `[data-theme='dark']` 里定义 rgba 令牌（参考 `plugins/totp/src/styles/app.css` 的 `--totp-*`），色值取自 `theme.css` 的 `--launcher-accent` / `--launcher-success`。

## 性能

- 解析、差分、格式化等 O(n) 以上或可能超过 50ms 的计算**必须**进 Web Worker，并保留主线程降级分支。
- 可能超过 200 行的列表**必须**虚拟滚动（用 `@launcher/ui/virtual`，定高行不需要测量）。
- 大文本/大 JSON 禁止 `JSON.parse` + `stringify` 往返（会丢数字精度与转义细节）；需要结构时用手写词法扫描。
- 表示树形结构**禁止**假设「兄弟节点下标连续」（先序遍历下不成立），用 `firstChild` + `nextSibling`。

## 收尾

- 改完**必须**跑：插件目录下 `npm run typecheck && npm run build && npm test`，仓库根 `pnpm spec-check`。
- 交付前**必须**用静态服务器打开 `dist/` 实机点一遍（生产构建的资源路径与 dev server 不同）。
- 新增 `no-view/` 脚本时，同步更新 `package.json` 的 `commands` 与 `build` 脚本。
