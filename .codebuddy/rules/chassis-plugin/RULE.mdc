---
description: 插件开发硬约束（plugins/ 下 13 个出厂插件；view 层 Vue / 逻辑层 Rust）：宿主调用直连 SDK、命令名与产物文件名必须一致、apiVersion 2 与 Rust crate 登记、单平台插件必须声明 platforms/arch、@launcher/ui 依赖解析、性能与安全的红线
alwaysApply: true
enabled: true
provider:
---

# 插件开发铁律（view 层 Vue / 逻辑层 Rust）

`plugins/` 下 13 个出厂插件（app-launcher / file-search / web-open / internal-settings / totp / host-manager / text-diff / json-tools / snips / calc-pad / markdown-notes / todo / screen-recorder）的硬约束。
**view 层仍是 Vite + Vue；`no-view` / `script` 逻辑层是 Rust 可执行产物（v2，ADR-0005）**——逻辑层 crate 根 = 插件目录，源码直接放该插件的 `src/`。
下面每一条都是「不遵守就一定出问题」的；完整背景见 `docs/plugin-dev-guide.md`（历史与踩坑），
操作流程见 skill `chassis-plugin-dev`。

## 宿主能力

- 宿主 API **直连 SDK**：view 侧 `import { exec, host, hostUi, screenshot, storage } from '@launcher/api'`；
  逻辑层（`no-view` / `script`）v2 用 Rust SDK `launcher-plugin-sdk`（v1 的 `@launcher/api-node` 已随 v1 内核一起删除）。
  双宿主适配层已于 2026-09-16 退役，**不要**再引 `packages/ui/lib/platform`。
- 每次调用都要想清楚失败路径：SDK 的 Promise 以 `LauncherError` 拒绝（不在宿主里时立刻 `NOT_FOUND`，宿主无响应则超时）。
  需要「失败即空值」就在调用点接 `.catch(() => null)`；`host.isLauncher()` 是**同步**判断，先用它分流。
- 剪贴板读取优先用 `paste` 事件的 `clipboardData`（用户手势，无需权限）；写入用 `writeText` → `execCommand` 三级兜底；UI 永远保留手工操作路径。
- 密钥、验证码等敏感数据默认不落明文；要持久化就提供口令加密（参考 `plugins/totp/src/core/vault.ts`）。

## 清单与产物

- `package.json` **必须**提供 `build:view` / `build:scripts` 脚本：根 `scripts/build-all.mjs` 按这两条驱动构建
  （只有 view 就只写 `build:view`；有 no-view/script 才写 `build:scripts`），否则 `pnpm build` 会跳过该插件。
- `commands[].name` 对 `no-view` / `script` 命令**必须**等于产物文件名（v2：可执行产物 `dist/<name>`，Windows `dist/<name>.exe`）。
- 有 `no-view` / `script` 命令 ⇒ `apiVersion` **必须** `"2"`（v1 的 `.mjs` 逻辑层产物内核不再支持；spec-check 的 N1 会校验可执行位 + 二进制魔数，脚本改名冒充不算）。
- **新逻辑层插件 = 建 crate + 登记根 `Cargo.toml` 的 members**（crate 名 `launcher-plugin-<id>`，`[[bin]] name` = 命令名）；漏登记 ⇒ `cargo build -p` 直接找不到包（members 不能写 `plugins/*` glob）。
- 逻辑层构建走 `build:scripts` = `cargo build --release -p <crate>` + `node ../../scripts/build-plugin.mjs <id> --copy-scripts`（带 view 的再加 `--keep-dist`，且顺序先 view 后 scripts；单一 bin 天然自包含，不存在 chunk 拆分问题。v1 的 `vite.worker.config.ts` + 逐入口构建是过渡期写法）。配套 `test:scripts` = `cargo test -p <crate>`。
- `vite.config.ts` **必须** `base: './'`，且不要启用 `manualChunks`。
- `dist/` 必须包含 `index.html` + `assets/` + `package.json`（含完整 `commands`）+ 每个逻辑层命令的可执行产物。
- **只在一个平台能跑的插件必须声明 `platforms`**（`["macos"]` / `["windows"]` / `["linux"]`，架构限制再加 `arch: ["x64"]` / `["arm64"]`；省略的维度 = 不限制）：内核在扫描期就整包跳过，用户不会看到「点开没反应」的插件，安装时也会明确报 `PLATFORM_MISMATCH**（plugin-spec §3.5）。取值照 Rust `target_os` 写（`macos` / `windows`），**不是** `host.info().platform` 的 `darwin` / `win32`。
  - 全平台插件**不要写**（写了反而要跟着平台扩展维护）；空数组非法（会被 `MANIFEST_INVALID` 拒）。
  - 只是**个别命令**依赖平台工具（如 `mdfind`）⇒ 用 `#[cfg(target_os)]` + 降级，不要整包声明平台。

## 逻辑层（`no-view` / `script`，v2，Rust）

- 产物是**可执行文件**（`dist/<name>`，Windows `.exe`），宿主 `spawn` + NDJSON over stdio（ADR-0005 / `plugin-spec` §4.4）；`.mjs` 产物不再支持。
- **必须**用 Rust SDK `launcher_plugin_sdk` 读写：`ctx.raw_args()` / `ctx.args::<T>()`、`ctx.settings_str()` / `ctx.settings_bool()`、`ctx.data_path()`（`ctx.plugin_path()` 只读）、`ctx.done(Value)` / `ctx.fail(e)` / `ctx.log(msg, data, Level)` / `ctx.progress(f, data)` / `ctx.on_query(..)` / `ctx.storage()`；panic 由 SDK 转 `fail`。
  手写 NDJSON 只允许出现在 SDK 自身与协议一致性测试里。
- **异步模式**：SDK 零依赖（纯 std 线程模型）。需要并发 / 超时的插件自建 `static RT: OnceLock<Runtime>` + `block_on`（参考 `file-search` / `app-launcher` 的 `runtime()`）；`on_query` handler 跑在 SDK 的 executor 线程、**不在任何 runtime 内**，所以 `block_on` 安全 —— 不要用 `Handle::current()`（会 panic）。
- stdout **只准**协议行（日志走 `ctx.log`，不要裸 `println!`）；stderr 由宿主转 warn 日志；野 stdout 行会被宿主按「协议解析失败」转入日志（终端里看不到，去宿主日志找）。
- 一次性执行：`ctx.done` 后必须退出；**不要**自己 daemon 化 / 常驻 —— 常驻能力走 `contributes: true`（宿主按需启停 + 空闲回收）。
- 失败语义：`run` 超时默认 10s（上限 5min，超时 `TIMEOUT`）；非零退出 / `fail` 只影响本次调用，连续 3 次该命令 `degraded`。
- **同一份纯逻辑被 view（TS）与逻辑层（Rust）双份实现时，必须建共享 fixture 向量**（输入 + 操作序列 → 期望输出，两侧测试都读它；参考 `plugins/host-manager/test/fixtures/blocks-vectors.json`）—— 双份实现漂移 = 预览与写入不一致（写系统文件时是危险事故）。
- 平台限定命令（`mdfind` / `sips` / `osascript` 等）在 Rust 侧用 `#[cfg(target_os = "macos")]` 隔离，并在清单 / 文档标注；M6 要补 Windows 后端（`docs/m5-rust-and-windows.md` §B2）。**整包**只在一个平台可用则改为声明清单的 `platforms`（见上一条），两者别混着用。

## 模块解析

- `@launcher/ui`（`packages/ui`）是工作区包：插件 `package.json` 声明 `"@launcher/ui": "workspace:*"` 后走标准解析，**不需要**再配 Vite alias / tsconfig paths。
- Tailwind v4 不会跨界扫描：`src/styles/app.css` **必须**用 `@source` 声明插件 `src` 与 `../../packages/ui` 两个范围，否则类名不生成。

## 响应式（大文档模型）

- 大结构用 `shallowRef` 持有、内部对象**原地改**时：**每次改动都要换新引用**（`doc.value = { ...doc, blocks: doc.blocks.map((b) => ({ ...b })) }`），
  不能只在原地 `push/splice` 之后补一个 `triggerRef` —— **`computed` 只在它读到的引用变了时才通知下游**，
  原地改之后 `blocks` 还是同一个数组、`props.block` 还是同一个对象，于是「列表 / 下拉选项 / 卡片里的条目」全部停在旧值上
  （2026-09-17 实测：新建的块在「收进块」弹窗里选不到、左栏也不出现；用 vue 响应式内核单测复现过：
  原地改 + `triggerRef` 下游永远收不到，换新引用立刻收到）。浅拷贝足够 —— 行对象与行数组共享，不复制内容。

## UI 与样式

- 表单控件写 `class="launcher-input"`（背景 / 边框 / focus 环跟着主题与主题色走）。
- **下拉框用 `@launcher/ui/UiSelect.vue`，不要写原生 `<select>`**：它的弹出菜单由 macOS 系统（NSMenu）绘制，不跟主题、不跟主题色。用法 `:model-value` + `@update:model-value`（`v-model` 可，数字字段自己 `Number()` 转）。
- 弹窗用 `@launcher/ui/UiDialog.vue`（自带遮罩 / Esc 关闭 / 进场），别自己叠一层。
- **消费 `Esc` 必须 `preventDefault()`**（`stopPropagation()` 也算）：没被消费的 Esc 由 SDK 自动交还宿主（退回启动台）。`UiDialog` / `UiSelect` 已内建，自己画的弹层 / 清空搜索框要照做。
- **进度类动画（倒计时环、进度条）只准有一个时间源**：JS 算目标值 + `transition` 补间；**不要**用 CSS 动画再每秒重设 `animation-delay`（动画自身时钟也在走 = 把同一秒算两遍，环一轮转两圈）。跨周期那一帧要临时关过渡，否则补间会把「空 → 满」演成倒转一整圈（参考 `plugins/totp/src/App.vue` 的 `ringOffset` / `markStepReset`）。
- 主题色派生的**半透明填充不要写 `color-mix`**：构建器（Lightning CSS）会把它拆成「实色兜底 + 真值」两条规则，实色兜底在老一点的 WebKit 上会把整块面刷成纯色（选中卡片直接变纯蓝）。写法：插件自己的 `:root` / `[data-theme='dark']` 里定义 rgba 令牌（参考 `plugins/totp/src/styles/app.css` 的 `--totp-*`），色值取自 `theme.css` 的 `--launcher-accent` / `--launcher-success`。

## 性能

- 解析、差分、格式化等 O(n) 以上或可能超过 50ms 的计算**必须**进 Web Worker，并保留主线程降级分支。
- 可能超过 200 行的列表**必须**虚拟滚动（用 `@launcher/ui/virtual`，定高行不需要测量）。
- 大文本/大 JSON 禁止 `JSON.parse` + `stringify` 往返（会丢数字精度与转义细节）；需要结构时用手写词法扫描。
- 表示树形结构**禁止**假设「兄弟节点下标连续」（先序遍历下不成立），用 `firstChild` + `nextSibling`。

## 收尾

- **发版（远程更新）**：改完要发更新就 bump `package.json` 的 `version`，合并 main 后 `gh workflow run plugins-release.yml --ref main -f plugins="<id>"`（通道细节见 skill `chassis-release`）。**essential 插件**（app-launcher / file-search / internal-settings）不参与远程更新，改它们只能随 App 发版；索引是整份发布的，单独发某个插件时工作流会自动合并上一版 `registry.json`（否则其余插件会从索引里消失）。
- 改完**必须**跑：插件目录下 `npm run typecheck && npm run build && npm test`；有逻辑层的加 `npm run test:scripts`（或仓库根 `cargo test --workspace` 一把跑），再跑仓库根 `pnpm spec-check`。
- 交付前**必须**用静态服务器打开 `dist/` 实机点一遍（生产构建的资源路径与 dev server 不同）。
- 新增逻辑层命令时，同步更新 `package.json` 的 `commands`、Cargo 的 bin、根 `Cargo.toml` members 与 `build:scripts` / `test:scripts`，并跑 `pnpm spec-check`（产物必须是可执行文件）。
