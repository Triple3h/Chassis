[package]
name = "launcher-plugin-sdk"
version = "0.1.0"
edition = "2021"
description = "Chassis 逻辑层插件 Rust SDK（apiVersion 2：子进程 + NDJSON over stdio）"
license = "MIT"
publish = false

[dependencies]
base64 = "0.22"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
